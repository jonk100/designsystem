---
/**
 * src/design/surfaces/section/Section.astro
 * A named region within a page establishing a background context.
 * - Operates by default transparently, but can accept a layer prop (e.g., --layer--1).
 * - Accepts padding props via the surface padding scale.
 * @props (local): as, layer, padding
 * @props (global): class, class:list, animate, onScroll
 */
import { mergeClasses, mergeStyles } from "@/design/shared/functions";
import type { SurfaceComponentProps } from "@/design/surfaces/surfaces.types";
import { SURFACE_PADDING_CLASS_MAP } from "@/design/surfaces/surfaces.maps";
import '@/design/surfaces/surfaces.css';
import './Section.css';

export interface Props extends SurfaceComponentProps {
}

const {
  as: Tag = 'section',
  layer,
  padding = 'xl',
  class: className,
  'class:list': classList,
  style,
  ...rest
} = Astro.props;

const classes = [
  'section-component',
  layer !== undefined && `surface-layer--${layer}`,
  SURFACE_PADDING_CLASS_MAP[padding]
];
const finalClasses = mergeClasses(classes, Astro.props);
const finalStyles = mergeStyles('', Astro.props);
---

<Tag class:list={finalClasses} style={finalStyles} {...rest}>
  <slot />
</Tag>